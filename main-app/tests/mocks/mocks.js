(function () {
    'use strict';
    var mocks = {
        //sandbox = sinon.sanbox.create();
        //audioApiMock = sinon.sandbox.mock(mocks.audioAPi);
        //module(function($provide){
        //    $provide.value('audioApi', mocks.audioApi);
        //});

        audioApi : {
            playerClick : function () {},
            playerMove : function () {}
        }
    }
})();